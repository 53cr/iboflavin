Given /^they have a Twitter account which is attached to an iBoflavin account$/ do
  pending
end

Given /^they are currently signed in to that Twitter account$/ do
  pending
end

Given /^they have a Twitter account which is not already attached to an iBoflavin account$/ do
  pending
end

When /^click "([^\"]*)" on the security prompt Twitter shows them,$/ do |arg1|
  pending
end

Then /^it should have my Twitter Screen Name, email, and Time Zone prefilled\.$/ do
  pending
end