%h1
  New goal
- form_for(@goal) do |f|
  = f.error_messages
  %p
    = f.label :nutrient_id
    %br
    = f.select :nutrient_id, Nutrient.select_options
  %p
    = f.label :amount
    %br
    = f.text_field :amount
  %p
    = f.submit 'Create'

= link_to 'Back', goals_path
